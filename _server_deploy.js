/**
 * One Time for your Project
 * ---------------------------------------
 * 1. Create Heroku Account
 * 2. Verify Email
 * 3. Install Heroku cli
 * 4. heroku Login (command)
 * 
 * For each Project one time
 * ---------------------------------------
 * 1. Check Project in Local Server
 * 2. heroku Create (command)
 * 3. Make sure you: git add. || git commit -m '' || git push
 * 4. git push heroku main (command)
 * 5. Go to Heroku Dashboard > Current Project > Settings > Reveal Config Vars
 * 6. Copy Paste config cars from your .enc file
 * 7. Make sure you have whitelisted all api address to access mongodb
 * 
 * Update server with new changes
 * ---------------------------------------
 * 1. Make Changes
 * 2. Make sure you: git add. || git commit -m '' || git push
 * 3. git push heroku main (command)
 */